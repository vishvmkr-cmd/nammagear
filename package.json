{
  "name": "student-gear-shop",
  "private": true,
  "pnpm": {
    "ignoredBuiltDependencies": ["sharp", "unrs-resolver"]
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:web": "pnpm --filter student-gear-shop-web dev",
    "dev:api": "pnpm --filter student-gear-shop-api dev",
    "build": "pnpm -r build",
    "start": "pnpm -r start",
    "db:push": "pnpm --filter student-gear-shop-api exec prisma db push",
    "db:migrate": "pnpm --filter student-gear-shop-api exec prisma migrate deploy",
    "db:generate": "pnpm --filter student-gear-shop-api exec prisma generate",
    "db:studio": "pnpm --filter student-gear-shop-api exec prisma studio",
    "db:seed": "pnpm --filter student-gear-shop-api exec prisma db seed"
  },
  "engines": {
    "node": ">=20"
  }
}
